{
  "scripts": {
    "json-server": "json-server --watch db.json --port 3000",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongoose": "^8.13.2"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
